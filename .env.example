# OVH FTP credentials (from OVH welcome email)
FTP_HOST=ftp.cluster100.hosting.ovh.net
FTP_USER=rslxggt
FTP_PASS=PASTE_YOUR_FTP_PASSWORD_HERE

# Use FTPS (encrypted FTP). Set to "false" only if FTPS fails.
FTP_SECURE=true

# Remote document root on OVH
REMOTE_ROOT=/www

# Token used to unlock the site for previewing.
# Visit https://rsl-x.gg/_unlock_<TOKEN> once to set the unlock cookie (valid 30 days).
# Pick something hard to guess. Alphanumeric + dash only.
UNLOCK_TOKEN=nb-2026-x7m2p9k4
